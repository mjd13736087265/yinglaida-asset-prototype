# Git 推送规范（所有模块开发助手共用，推送前必读）

> 用途：本项目的原型统一发布到同一个 GitHub 仓库。任何助手需要推送时，按本规范执行，**不要反复向业主确认账号**。

## 1. 默认账号与仓库（已定稿，直接使用，勿再问）

- GitHub 账号：**mjd13736087265**（业主默认账号，所有推送一律用这个）
- 仓库：`mjd13736087265/yinglaida-asset-prototype`，分支 `main`
- 线上地址（GitHub Pages）：https://mjd13736087265.github.io/yinglaida-asset-prototype/
- 本地提交身份：`kimi <kimi@local>`（沿用历史提交，仓库级配置即可，勿动全局）

## 2. 本地发布暂存区

- 路径：`02-需求设计/_临时文件/_repo/`（工作区 `02-需求设计` 本身不是 git 仓库，发布一律走 `_repo`）
- `_repo` 是上述仓库 main 分支的浅克隆，只放要发布的文件（原型 html / 共享 assets / 交接文档 / 规范文档），`_临时文件` 里的脚本、截图、日志不进仓库

## 3. 标准推送流程

1. 同步：把自己负责的、有改动的文件从工作区复制到 `_repo` 对应路径
2. 核对：`git status --short` + `git diff --cached`，确认改动清单只有自己的部分——**特别检查不得误删或回退其他助手的文件**（片区、主页等并行模块）
3. 提交：`git commit`，消息写清模块名 + 改动要点
4. 冲突处理：push 被拒（远程有他人新提交）时，`git fetch` → 逐文件比对远程与工作区 → 确认无冲突后 `git reset --soft/hard origin/main` 重新叠加自己的文件再提交推送；**禁止 `push --force`**
5. 验证：push 成功后等约 1 分钟，FetchURL 线上地址确认内容生效

## 4. 何时才需要问业主

- 要推送到**其他账号或其他仓库**时
- 要执行 force push、删除远程分支、删仓库文件等不可逆操作时
- 凭证失效、403/认证失败时（让业主重新授权一次即可）

除以上三种情况，按本规范直接推送，不再逐步请示。

## 5. 说明

- 本机 git 凭据由 Windows 凭据管理器缓存，正常情况推送无需再登录；若弹出账号选择，选 mjd13736087265 即可
- 工具层面的操作审批弹窗属于客户端安全机制，与本规范无关，助手应把多个小操作合并成尽量少的命令，减少打扰
