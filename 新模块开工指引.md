# 新模块开工指引（发给接手剩余模块的 AI · 开工前必读）

> 用途：你将与主会话（及其他模块助手）**共用同一个工作文件夹** `D:\WORK\KIMI WORK\物管系统\02-需求设计`。本文件划定你的行动边界，读完并按顺序读完指定文档后再动手。

## 0. 你的任务

- 在 `02-需求设计` 下新建并负责分配给你的模块（待开发：水电管理 / 物业报修 / 客房清扫 / 数据报表 / 审批中心，具体以业主分配为准）
- 目录命名沿用现有约定：`06-水电管理/原型/`、`07-物业报修/原型/` 依次编号（编号以业主确认为准）
- 每个模块按三端产出页面：PC 管理端（`pc-*.html`）、移动管理端（`m-admin-*.html`）、移动用户端（`m-user-*.html`，按需）

## 1. 必读文档（按此顺序，读完再开工）

1. `原型对接规范.md` —— 系统与端、目录约定、硬性规则、统一演示数据、导航组件挂载写法（§7）、编号规则（§9.1，全系统统一）、交付 SOP（§10）
2. `Git推送规范.md` —— 推送一律走 Kimi GitHub 插件，禁止 git push / 弹窗登录；**仓库路径不带 `02-需求设计/` 前缀**（§3 路径映射红线）
3. `00-整体需求/主页原型/assets/app.css` —— 共享设计系统，先看有哪些现成类，别重造
4. `模块交付清单模板.md` —— 每次交付/更新必须按此模板附交付清单

## 2. 边界红线（最重要）

**你只能新建/修改自己模块目录内的文件**（如 `06-水电管理/**`）。

以下文件和目录**一律不许碰**，由主会话统一维护：

- `00-整体需求/` 整个目录：三个主页（`pc-home.html` / `m-admin-home.html` / `m-user-home.html`）、共享资产 `assets/`（`app.css` / `nav.js` / `m-tab.js`）、`mobile-board.html`
- 根目录 `index.html`（线上门户）、`原型对接规范.md`、`Git推送规范.md`、`模块交付清单模板.md`
- 其他模块目录：`01-系统管理/`、`02-片区管理/`、`03-收费管理（APP）/`、`03-收费管理（PC）/`、`04-租务运营/`、`05-合同管理/`
- `_临时文件/` 整个目录（含 `_repo` 发布基线，是主会话的推送工具）
- `tools/`（可运行 `class_audit.py` 做类审计，但不许修改）

**违反后果**：共享文件被改会污染所有已上线模块，主会话会回退你的改动并要求返工。

## 3. 需要你模块之外配合时，这样做

| 需求 | 做法 |
|---|---|
| app.css 没有你要的组件/样式 | 先页面级 `<style>` 解决；确属通用组件的，写进交付清单 §4「共享组件申请」，由主会话评审后加入 app.css |
| 主页/导航挂你模块的入口 | **不要自己改主页和 nav.js/m-tab.js**。在交付清单里写明期望入口（入口名、图标建议、目标页路径），主会话统一接通 |
| 需要跳转到其他模块的页面 | 可以链接（相对路径，参考 `原型对接规范.md` §2 目录约定），并在交付清单 §7 登记为跨模块对接点 |
| 对其他模块页面有疑问 | 写进交付清单 §7「遗留与待确认」，不要替对方改 |

当前主页已预留的占位入口（ dead 格子，等你们模块上线后由主会话接通）：移动管理端「全部应用」宫格的水电管理 / 物业报修 / 客房清扫 / 数据报表 / 审批中心，以及「常用功能」的报修派单。

## 4. 风格与数据一致性要点

- 所有页面只用 app.css 已定义的 CSS 变量取色，页面级补充样式放 `<style>`，禁止内联写死色值（交付前跑 `python tools/class_audit.py` 自查）
- 演示数据与全系统保持一致：片区名、租户、金额等见 `原型对接规范.md` §5；涉及合同号/账单号时遵守 §9.1 编号规则（含片区编号段）
- PC 页挂 `nav.js`、移动页挂 `m-tab.js`，挂载写法和 active id 对照见 §7——照抄即可，不要改组件本身
- 线上已上线模块（片区/收费/租务/合同）的页面是风格基准，不确定时打开对应页面参照：https://mjd13736087265.github.io/yinglaida-asset-prototype/

## 5. 交付与推送

1. 完工后按 `模块交付清单模板.md` 填交付清单（基线声明、改动清单、三端页面清单、自测证据缺一不可）
2. 推送只推**自己模块目录**内的文件，走 GitHub 插件 `push_files`，commit 消息写「模块名 + 改动要点」
3. 路径红线再强调：`push_files` 的 path 是仓库根相对路径，如 `06-水电管理/原型/pc-xxx.html`，**不要带 `02-需求设计/` 前缀**，推完用 `get_file_contents` 核对
4. 推送后约 1 分钟 FetchURL 线上地址验证，然后通知业主「可对接」，由主会话做入口接通和全站核对

## 6. 协作约定

- 工作文件夹多会话共用：不要批量重命名/移动/删除任何不属于你模块的文件
- 不确定某件事归谁做 → 默认归主会话，写进交付清单 §7，别自己动手
- 有疑问先问业主，不要猜测其他模块的业务口径
