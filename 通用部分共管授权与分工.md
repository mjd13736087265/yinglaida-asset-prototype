# 通用部分共管授权与分工（2026-09-20 起生效）

> 用途：业主授权**两个 AI 会话共同管理**通用部分。本文件定义通用部分的范围、管理职责和共管协议。任何获准管理通用部分的会话，动手前必读本文 + `原型对接规范.md` + `Git推送规范.md`。

## 1. 通用部分清单（文件级，除此之外都归各模块自己）

| 类别 | 文件/目录 |
|---|---|
| 主页三端 | `00-整体需求/主页原型/`：`pc-home.html`、`m-admin-home.html`、`m-user-home.html`、`mobile-board.html` |
| 共享资产 | `00-整体需求/主页原型/assets/`：`app.css`（设计系统）、`nav.js`（PC 导航）、`m-tab.js`（移动 Tab） |
| 门户 | 根目录 `index.html` |
| 规范文档 | `原型对接规范.md`、`Git推送规范.md`、`新模块开工指引.md`、`模块交付清单模板.md`、本文件 |
| 工具 | `tools/`（如 `class_audit.py`） |
| 发布基线 | `_临时文件/_repo/`（git 克隆，用于 diff 与推送） |

## 2. 管理职责（通用部分管理员要做的事）

1. **共享组件评审**：各模块通过交付清单 §4 申请的新组件，评审后加入 `app.css`，并同步通知所有模块可用
2. **入口接通**：模块上线后，统一把入口挂到主页宫格/常用功能和 nav.js、m-tab.js（模块方不许自己改）
3. **规范维护**：口径变化（如编号规则、演示数据）及时更新规范文档并注明版本/日期
4. **跨模块仲裁**：对接点登记（规范 §8）、模块间口径冲突的裁定建议（最终由业主拍板）
5. **通用文件的推送与验证**：按 `Git推送规范.md` 走插件推送，推后核对

## 3. 共管协议（两个会话防冲突，务必遵守）

1. **一次只动一个会话**：任何会话要改通用文件前，先向业主声明「我要改 XX 文件」；业主确认另一个会话当前没有进行中的通用改动后再动手
2. **改完即推送、即通知**：通用文件改动完成后当次推送上线，并把「改了什么文件、为什么、commit 号」告诉业主，由业主转达另一个会话
3. **不追平对方进行中状态**：拿不到对方会话上下文是正常的，一切以**仓库远程 main 为唯一事实源**；动手前先用插件 `get_file_contents` 拉远程最新版，不要信本地缓存
4. **冲突处理**：发现远程已被对方改过的迹象（内容与自己预期不符），停下来报给业主，不覆盖、不回退
5. **模块文件无权碰**：通用部分管理员也不许改各模块目录内的文件（`01`~`07` 等），发现模块问题写进对接记录让模块方自己改

## 4. 变更记录（强制）

每次改通用文件，commit 消息格式：`通用：改了什么（原因）`。涉及规范口径变化的，同时在对应规范文档里更新版本/日期行。

## 5. 省额度工作方式（业主要求）

- 小步交付：一次只做一类改动，避免长会话反复上下文加载
- 验证从简：能用 `get_file_contents` / FetchURL 核对的，不截图；确需截图一次到位
- 问题批量提：模块方把多个疑问攒在交付清单 §7 一次提，不逐条开对话
- 日常小改（换文案、接通单个入口）由通用管理员会话直接做；涉及多模块的大整合再开专项对话
