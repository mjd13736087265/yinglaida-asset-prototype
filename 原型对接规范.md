# 原型对接规范（每开新模块对话前必读）

> 用途：本系统按模块分对话迭代原型。任何新模块开工前，先读本文件 + 共享设计系统 `00-整体需求/主页原型/assets/app.css`，即可保证风格与结构一致。

## 1. 系统与端

- 系统：英莱达资产管理系统（物业/园区公寓租赁，本期只做公寓业态）
- 命名规则：PC 端与封面/导航页用全称「英莱达资产管理系统」，移动端（微信小程序）用简称「英莱达资管」，用户端租户侧产品线名「英莱达公寓」保留不变
- 三端：PC 管理后台（1440 宽设计）、微信小程序管理端、微信小程序用户端（375 手机壳展示）
- 用户画像：年纪稍长的物管人员——大字号、大触点、层级浅、高对比

## 2. 目录约定

```
02-需求设计/
  index.html + package.json + vite.config.js   # ★ 统一预览入口（服务根必须在这一层，
                                               #   否则跨目录引用 app.css 会 404、页面退化为纯文字）
  00-整体需求/主页原型/        # 主页原型 + 共享资产（唯一设计系统源）
    assets/app.css             # ★ 所有模块唯一引用的样式文件
    index.html                 # 封面导航（新模块在此加卡片）
    pc-home.html               # PC 主页（侧边栏预留位在此接入）
  01-系统管理/                 # 模块目录（待做）
  02-片区管理/
    原型/                      # 模块页面统一放「原型」子目录
      index.html               # 模块封面导航
      pc-*.html / m-*.html     # 页面命名：pc- 后台、m-admin- 移动管理端、m-user- 移动用户端
      package.json + vite.config.js   # 复制模板即可（支持跨目录引用 app.css）
```

## 3. 新模块开工三步

1. **读**：本文件 + `app.css`（类清单即组件库）；可参考 `pc-home.html` 的侧边栏/顶栏写法
2. **写**：页面放模块 `原型/` 目录，样式引用 `../../00-整体需求/主页原型/assets/app.css`；app.css 没有的组件，先由主会话补充进 app.css（禁止各模块自创重复组件，防止风格漂移）；PC 侧边栏与移动端底部 Tab 一律挂载共享组件，写法见第 7 节
3. **接**（做完后回接主页）：
   - `pc-home.html` 侧边栏：对应菜单去掉 `.lock` 占位，包上链接
   - `主页原型/index.html` 封面加模块卡片
   - 移动端宫格入口按需换链接

## 4. 硬性规则

- 禁止外部图片/字体/CDN，图标一律手绘 inline SVG（viewBox 24、stroke 1.6、圆角线帽）
- 颜色只用 app.css 的 CSS 变量；数字加 `.num` 类；演示数据三端保持一致
- 房态/状态类颜色与文案均标注 .note：「来源于 系统管理-字典配置」
- 后做的模块只画预留位（占位 + 备注），不展开设计
- 验证：无头浏览器截图目检每个页面后才算完成；预览依赖 `npm run dev`（vite）
- ★ 片区数据隔离（全系统红线，渗透所有业务模块）：
  1. 所有业务列表/台账/统计默认只显示当前登录人有权限的片区；权限来源于 系统管理-数据权限（按片区授权），各模块只消费不做权限配置
  2. PC 每页顶部放全局片区选择器（切换=整页数据刷新）；小程序端放页面第一行；演示账号陈志远为运营主管可见全部 6 片区（含"全部片区"选项）
  3. 列表页配业态筛选器：本期默认选中"公寓"，其他业态（写字楼/商业/厂房）画预留位（灰置+.note「待 M10/M11」）
  4. 页面所有金额/统计卡片的数据口径=当前所选片区，并加 .note「数据范围=当前所选片区」
  5. 涉及编号的单据（收据/账单等），编号规则含片区编号段，各片区独立流水

## 5. 统一演示数据（各模块保持一致）

- 登录人：陈志远（运营主管）；片区 6 个，房源 1,248 间（公寓），出租率 87.6%，本月实收 96.40 万 / 应收 105.7 万 / 回款率 91.2%，欠费 5.76 万（23 户），待办 6
- 层级模型：片区（L1，园区/项目）→ 业态（L2，经营单元）→ 房源（L3，楼栋/楼层/房间）；一个片区允许有多个同类型业态，原型只演示每片区 1 个公寓业态；本期仅「公寓」业态可进入管理，酒店 / 停车场 / 商业等业态一律置灰占位 +「预留」角标，不可点击；业态类型来源于 系统管理 - 字典配置
- 片区 → 业态映射：兴业人才公寓 → 兴业公寓（占位：停车场）｜云谷创业园区 → 云谷人才公寓（占位：酒店、停车场）｜学府青年园区 → 学府青年公寓（占位：商业）｜临港智造园区 → 临港人才公寓（占位：停车场）｜滨江科创园区 → 滨江人才公寓（占位：商业、停车场）｜梧桐公寓楼 → 梧桐公寓（占位：停车场）
- 片区地址 / 负责人：兴业人才公寓（城西区兴业路 66 号 · 李慧敏）｜云谷创业园区（余杭区云谷路 199 号 · 张伟东）｜学府青年园区（西湖区学府街 45 号 · 陈国强）｜临港智造园区（临港区海港大道 500 号 · 刘秀英）｜滨江科创园区（滨江区江南大道 1288 号 · 王建国）｜梧桐公寓楼（老城区梧桐巷 12 号 · 赵明远）
- 片区房量 / 出租率：兴业人才公寓 320 间 92.3%｜云谷创业园区 260 间 88.5%｜学府青年园区 240 间 85.0%｜临港智造园区 98 间 90.2%｜滨江科创园区 208 间 86.1%｜梧桐公寓楼 122 间 79.5%
- 房态：在租 1,093 / 空置 98 / 即将到期 41 / 维修中 16（预留：已预订）
- 片区详情示例：列表/房态页统一用「滨江科创园区 · 滨江人才公寓」（1 栋 8 层×10 间、2 栋 8 层×10 间、3 栋 6 层×8 间 = 208 间）；片区详情（业态列表）与编辑业态页统一用「云谷创业园区 · 云谷人才公寓」（演示多业态占位：酒店 + 停车场；楼栋结构 10×10 + 10×10 + 6×10 = 260 间）
- 配套设施（展示层，预置字典 12 类）：空调、热水器、淋浴、独立卫生间、洗衣机、冰箱、床、衣柜、书桌、WiFi、简易厨房、阳台；演示勾选 8 个：空调、热水器、淋浴、独立卫生间、床、衣柜、书桌、WiFi
- 房间资产（验收层，演示 5 行）：美的空调 KFR-35GW（电器 ×1 · 完好 · 壁挂式·2019 年购置）｜海尔电热水器 60L（电器 ×1 · 完好）｜实木床 1.5m 含床垫（家具 ×1 · 完好）｜双门衣柜（家具 ×1 · 损坏 · 柜门合页松动，退住验收时定责）｜淋浴花洒套装（卫浴 ×1 · 完好）
- 用户端演示租户：张女士 = 滨江科创园区 · 滨江人才公寓 1 栋-301（在租，¥1,850/月，合同期 2025-12-01 ~ 2026-11-30）+ 2 栋-401（即将到期，¥1,380/月，合同至 2026-09-19 · 剩 15 天）；月租金合计 ¥3,230；「我的房源」从用户端首页「我的服务」宫格进入，缴租 / 报修 / 看合同为后续模块预留入口

## 6. GitHub 线上部署（2026-09-07 已完成）

- 仓库：https://github.com/mjd13736087265/yinglaida-asset-prototype （public，分支 main）
- 线上地址：https://mjd13736087265.github.io/yinglaida-asset-prototype/ （GitHub Pages，source = main 根目录，已加 .nojekyll）
- 仓库内容 = `02-需求设计/` 下的纯静态文件（各 html + app.css），不含 node_modules / package.json / vite.config.js / xlsx / md
- 新模块上线方式：用 GitHub MCP 的 push_files 把新增/修改的 html（仓库内路径 = 相对 `02-需求设计/` 的路径）推到 main 分支即可，Pages 约 1 分钟自动更新；推送后须对线上 URL 截图复核
- 页面间链接全为相对路径，线上/本地行为一致，无需为部署改代码

## 7. 通用导航组件（2026-09-08 起强制）

PC 侧边栏与移动端底部 Tab 已抽成共享 JS 组件，菜单/Tab 结构改一次全站生效。**禁止在新页面手写侧边栏或底部 Tab。**

- PC 侧边栏组件：`00-整体需求/主页原型/assets/nav.js`
- 移动底栏组件：`00-整体需求/主页原型/assets/m-tab.js`

### 挂载写法

```html
<!-- PC 页：.layout 首位放挂载点，body 末尾引脚本 -->
<aside class="sidebar" id="amsNav" data-active="area"
       data-ver="V1.1 原型 · 本期范围：片区管理"></aside>
<script src="../../00-整体需求/主页原型/assets/nav.js"></script>

<!-- 移动页：.phone 内、.screen 之后放挂载点，body 末尾引脚本 -->
<div class="m-tab" id="amsTab" data-variant="admin" data-active="area"></div>
<script src="../../00-整体需求/主页原型/assets/m-tab.js"></script>
```

- `data-active`：当前页菜单/Tab id，组件据此高亮（渲染为 `.mi.on` / `.ti.on`，不可点）；命中子菜单 id 时该子项高亮，父级组默认展开并加 `.parent-on`
- 带子菜单的组（children）：一级行点击 = 展开/收起（toggle，不跳转），箭头随状态旋转；当前页所在组默认展开，其余组默认收起
- `data-ver`（仅 PC）：侧边栏底部版本行文案，缺省 `V1.1 原型`
- `data-variant`（仅移动）：`admin` = 管理端 4 Tab（工作台/片区/收租/我的）；`user` = 用户端 4 Tab（首页/房源/消息/我的）
- 组件基于 `document.currentScript.src` 推算路径（截到 `主页原型/` 为 base，`base + '../../'` 为站点根），任意目录深度可直接挂载，GitHub Pages 子路径部署可用；禁止用 `/` 开头的绝对路径
- JS 渲染侧边栏/底栏，截图验证时需等渲染（Edge 无头截图默认会等）

### 菜单唯一数据源

- PC 菜单唯一数据源 = `nav.js` 顶部 `MENU` 数组；移动 Tab 唯一数据源 = `m-tab.js` 顶部 `TABS` 对象
- **新模块上线时：必须把对应菜单项的 soon 占位换成真实 href 或 children**（收费管理已解锁：`id:'rent'` 组 text=「收费管理」+ 8 个 children 子菜单），并同步解锁 `m-tab.js` 对应 Tab（移动端页面开工时再解锁）

### active id 对照表

PC（nav.js）：
- 已用：`home`（工作台）｜`area`（片区管理：pc-area-list / pc-area-detail / pc-area-edit / pc-format-edit）｜`room`（房源管理：pc-room-list / pc-room-edit / pc-room-import）｜`map`（房态监控：pc-room-map）
- 收费管理（rent 组，含子菜单）：`charge-home`（收费工作台）｜`charge-bills`（账单管理；pc-bill-detail 账单详情归位高亮本 id）｜`charge-verify`（收款核销）｜`charge-overdue`（欠费管理）｜`charge-deposit`（押金管理）｜`charge-special`（特殊处理）｜`charge-invoice`（票据管理）｜`charge-settings`（规则配置）
- soon 占位 id：`contract`（合同管理）｜`meter`（水电管理）｜`service`（物业服务（报修））｜`approval`（审批中心）｜`system`（系统管理）

移动（m-tab.js）：
- admin 变体：`home`（工作台）｜`area`（片区）｜`rent`（收费，已解锁 → 03-收费管理（APP）/管理端/原型/m-admin-charge-home.html，收费各移动端二级页 data-active="rent" 高亮归位）｜`my`（我的，占位）；二级页 `map` / `room` 归位高亮 `area`
- user 变体：`home`（首页）｜`rooms`（房源）｜`msg`（消息，占位）｜`my`（我的，占位）；`room`（房源详情）归位高亮 `rooms`；`m-user-myrooms` 页按原设计不高亮（不写 data-active）

## 8. 跨模块对接点登记（2026-09-17 起）

| 提供方 | 消费方 | 对接内容 | 备注 |
|--------|--------|---------|------|
| 收费管理 · 账户管理（#19） | 收费管理 · 收款登记 | 收款登记的「收款账户」下拉读取账户管理维护的数据 | 收费管理对话留言登记；账户数据本身在片区管理侧业态层级维护（收款抬头 + 收款账户），两模块字段口径需保持一致 |
| 片区管理 · 房源 | 合同管理（未开工） | 房源仅回显/跳转：查看关联合同 → 合同模块；查看承租人信息 → 租客信息模块（与合同模块分开） | 房源侧不做合同上传入口 |
| 片区管理 · 房源资产 | 资产管理（后续模块） | 房源页资产只读展示 + 置灰「＋绑定资产（待资产模块上线）」按钮 | 2026-09-11 纪要：资产照片/价格/详情均在资产模块配置 |
| 收费管理 · APP 管理端 | 主页原型 · 移动管理工作台 | 工作台宫格「收租管理」更名「收费管理」并接通 → `03-收费管理（APP）/管理端/原型/m-admin-charge-home.html` | ✅ 2026-09-17 已接通并实测跳转正常 |
| 收费管理 · APP 用户端 | 主页原型 · 移动用户端首页 | 宫格「待缴账单」→ `m-user-bills.html`；「开票申请」更名「凭证申请」→ `m-user-voucher.html`（路径均在 `03-收费管理（APP）/用户端/原型/`） | ✅ 2026-09-17 已接通并实测跳转正常；9·17 定稿：用户端不出现"发票"字样入口 |
